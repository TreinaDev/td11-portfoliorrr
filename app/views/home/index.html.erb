<div class="container">
  <div id="vue-app" class="card">
    <div class="card-body">
      <p>
        Porfoliorrr é uma rede social em que pessoas podem cadastrar suas informações e formações,<br>
        além de fazer postagens próprio para exibir seu trabalho ao mundo.<br>
        É seu currículo e é seu portfólio, é o
      </p>
      <h1>Portfoliorrr</h1>
    </div>
  </div>

  <% if user_signed_in? %>
    <div class="row">
      <section class="col-md-9 y-5">
        <h2>Feed</h2>
        <% if current_user.profile.followed_count > 0 %>
          <% if @followed_posts.any? %>
            <div>
              <%= render partial: 'posts/listing', locals: { posts: @followed_posts.each } %>
            </div>
          <% else %>
            <div>
              <h4>Não existem posts de perfis que você segue. Veja o que outras pessoas estão publicando:</h4>
              <%= render partial: 'posts/listing', locals: { posts: @posts } %>
            </div>
          <% end %>
        <% else %>
          <div>
            <h4>Você ainda não segue ninguém. Siga alguém para personalizar seu feed</h4>
            <%= render partial: 'posts/listing', locals: { posts: @posts } %>
          </div>
        <% end %>
      </section>

      <aside class="col-md-3 my-5">
        <h2>Usuários mais seguidos</h2>

        <div style="width: 18rem;">
          <% @most_followed.each do |profile| %>
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= profile.full_name %></h5>
                <p class="card-text"><%= profile.followers_count %> seguidores</p>
                <a href="<%= profile_path(profile) %>" class="btn btn-primary">Ver perfil</a>
              </div>
            </div>
          <% end %>
        </div>
      </aside>
    </div>
  <% elsif current_user.nil? %>
    <section>
      <h2>Feed</h2>
      <div>
        <h4>Faça login ou crie uma conta para descobrir conteúdos relevantes para você.</h4>
      </div>
    </section>
  <% end %>
</div>
