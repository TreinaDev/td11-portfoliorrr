<% if @professional_info.errors.any? %>
  <p>Verifique o(s) erro(s) abaixo:</p>
    <ul> 
      <% @professional_info.errors.full_messages.each do |msg| %>
        <li><%= msg %> </li>
      <% end %>
    </ul>
  <% end %>
